This is a basic network packet sniffer I built using Python and the Scapy library. It captures live network traffic and shows details like source IP, destination IP, and the protocol being used (TCP, UDP, or ICMP) for each packet.

I made this project to understand how network traffic works and how packets move between devices. It's a simple beginner-level tool and should only be used on networks you own or have permission to monitor.
